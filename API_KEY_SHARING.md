# 🔑 API 키 자동 공유 기능

## 개요

특허 번역 시스템을 여러 프로젝트에 설치할 때 **API 키를 매번 설정할 필요가 없습니다**!

설치 스크립트가 자동으로 원본 프로젝트의 API 키를 새 프로젝트에 복사합니다.

---

## ✨ 작동 방식

### 1. 설치 시 자동 복사

```bash
./install.sh "/path/to/new-project"
```

→ 원본 프로젝트의 `.env` 파일이 자동으로 복사됨
→ API 키 설정 완료! 🎉

### 2. 설치 후 즉시 사용 가능

```bash
cd "/path/to/new-project"
./run_gui.sh
```

→ API 키 설정 없이 바로 번역 가능!

---

## 🔄 API 키 업데이트

원본 프로젝트에서 API 키를 변경한 경우, 다른 모든 프로젝트에 한 번에 적용할 수 있습니다.

### 사용법

```bash
cd "/Users/earendel/Library/CloudStorage/OneDrive-개인/경력관리/SJ/Translation/JLT/patent-translation-system"

./update_api_key.sh \
  "/path/to/project1" \
  "/path/to/project2" \
  "/path/to/project3"
```

### 예시

```bash
./update_api_key.sh \
  "/Users/earendel/Library/CloudStorage/OneDrive-개인/경력관리/SJ/Translation/SuccessGlo" \
  "/Users/earendel/Library/CloudStorage/OneDrive-개인/경력관리/SJ/Translation/베링랩"
```

결과:
```
========================================
API 키 업데이트 스크립트
========================================

✓ 소스 .env 파일 확인됨

처리 중: /path/to/SuccessGlo
  ✓ API 키 업데이트 완료

처리 중: /path/to/베링랩
  ✓ API 키 업데이트 완료

========================================
✅ 모든 프로젝트 업데이트 완료!
========================================
```

---

## 🛡️ 보안 고려사항

### .env 파일 보호

각 프로젝트의 `.gitignore`에 자동으로 추가됩니다:

```gitignore
# API 키 및 환경 변수
.env
*.env
```

→ Git에 커밋되지 않습니다!

### 권한 설정

설치된 `.env` 파일은 사용자만 읽을 수 있습니다:

```bash
-rw-r--r--@  1 user  staff  257 Nov 15 03:09 .env
```

---

## 📋 체크리스트

### 처음 설치할 때

- [ ] 원본 프로젝트에 `.env` 파일이 있는지 확인
- [ ] `.env` 파일에 유효한 API 키가 있는지 확인
- [ ] `./install.sh [대상_경로]` 실행
- [ ] 설치 완료 메시지에서 "✓ API 키 자동으로 설정되었습니다!" 확인

### API 키 변경 후

- [ ] 원본 프로젝트의 `.env` 파일 수정
- [ ] `./update_api_key.sh` 스크립트 실행
- [ ] 모든 대상 프로젝트 경로 지정
- [ ] "✅ 모든 프로젝트 업데이트 완료!" 확인

---

## 🔧 문제 해결

### 문제: API 키가 복사되지 않음

**원인**: 원본 프로젝트에 `.env` 파일이 없음

**해결**:
```bash
cd "/Users/earendel/Library/CloudStorage/OneDrive-개인/경력관리/SJ/Translation/JLT/patent-translation-system"

# .env 파일 생성
cat > .env << EOF
# Anthropic API Key
ANTHROPIC_API_KEY=sk-ant-your-actual-key-here
EOF
```

그 다음 다시 설치하거나 `update_api_key.sh` 실행

### 문제: 설치 후 API 오류 발생

**원인**: API 키가 유효하지 않음

**해결**:
1. 원본 프로젝트의 `.env` 파일 확인
2. API 키가 `sk-ant-`로 시작하는지 확인
3. Anthropic Console에서 키가 활성화되어 있는지 확인
4. `./update_api_key.sh`로 재배포

---

## 💡 추가 팁

### 여러 프로젝트에 한 번에 설치

```bash
# 설치 스크립트를 여러 번 실행
./install.sh "/path/to/project1"
./install.sh "/path/to/project2"
./install.sh "/path/to/project3"

# API 키는 자동으로 모든 프로젝트에 복사됨!
```

### 프로젝트별로 다른 API 키 사용

특정 프로젝트만 다른 API 키를 사용하려면:

```bash
cd "/path/to/special-project"
nano .env  # API 키 수정
```

→ 해당 프로젝트만 다른 키 사용, 나머지는 원본 키 유지

---

## 🎯 사용 사례

### 사례 1: 새 프로젝트 시작

```bash
# 1. 새 프로젝트에 설치
./install.sh "/Users/earendel/Documents/NewProject"

# 2. 바로 이동 및 실행 (API 키 설정 불필요!)
cd "/Users/earendel/Documents/NewProject"
./run_gui.sh

# 3. 번역 시작
```

### 사례 2: API 키 갱신

```bash
# 1. 원본 프로젝트에서 키 변경
nano .env

# 2. 모든 프로젝트에 배포
./update_api_key.sh \
  "/path/to/project1" \
  "/path/to/project2" \
  "/path/to/project3"

# 3. 모든 프로젝트에서 새 키 사용 시작
```

---

## ✅ 요약

| 작업 | 명령어 | API 키 설정 필요? |
|------|--------|-------------------|
| 새 프로젝트 설치 | `./install.sh [경로]` | ❌ 자동 복사 |
| API 키 업데이트 | `./update_api_key.sh [경로들...]` | ❌ 자동 배포 |
| 프로젝트별 커스터마이징 | `nano .env` | ✅ 수동 편집 |

**결론**: 한 번 설정하면 모든 프로젝트에서 자동으로 사용! 🎉
